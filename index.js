const define = function() {};
const ablover = function xmath() {
  x = Math.floor[(1 + 2*(0 * 3))]
}

var leaterrr = new define(
  define(new function def() {
    ablover(define(1 + 2*(4 + 1) || x * 3))
  })
)

var definer = function() {};